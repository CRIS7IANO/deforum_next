# Dope Sheet selection + ripple (v18)

Dope Sheet improvements:
- Ctrl/Cmd click to multi-select rows (track/channel pairs)
- Shift click to range-select rows
- Box-select: drag on empty area to select rows by y-intersection

Ripple tool:
- Shifts keys **after** a given frame by a delta
- Scope: selected rows or all rows
