# Shot Overrides UI (v10)

The editor shows a **Shot Overrides** panel.

- Shots are computed from `timeline.cuts`.
- You can set per-shot overrides:
  - `cfg`
  - `steps`
  - `sampler`

These are written into `timeline.shots` and exported via `export-a1111-shots`.
